{
    "tasks": [
        {
            "type": "shell",
            "label": "go build",
            "command": "go",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "args": [
                "build",
                "-o",
                "cros-repo",
            ],
        },
    ]
}